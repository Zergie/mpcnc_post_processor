[gcode_macro RELAY_OFF]
description: Turn all via relay connected devices off
gcode:
  {% for key, value in printer.configfile.config %}
    {% if key.startswith("output_pin RELAY_") %}
      SET_PIN PIN={key.split(" ")[1]} VALUE=0
    {% end %}
  {% endfor %}
